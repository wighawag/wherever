2026-09-09 — `server/test/folder-conflict-continue.test.ts` failed once with `Error: server did not become healthy` from `startHarness` (`server/test/harness.ts:223`) during a FULL `vitest run` of the server suite; the same file passes in isolation in ~9s. The health-poll deadline in the harness looks too tight for a machine already running several server children in parallel. Noticed while building `restore-create-folder-action`; not investigated, not fixed.

2026-09-10 — reproduced at scale while running the suite for `new-session-clone-via-registry`: on a box at load average ~20-30 (16 cores), the SAME `server did not become healthy` error failed 21 of 28 files / 56 of 211 tests in one run, and 3 files in a lighter one; every affected file passes in isolation. So the deadline is load-sensitive rather than file-specific, which makes any full-suite gate on a shared machine unreliable until the poll deadline is raised or made adaptive.
